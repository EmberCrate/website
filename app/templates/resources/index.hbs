{{page-title 'All resources'}}

<Routes::Resources::IndexPage
  @model={{@model}}
  @filter={{this.filter}}
  @sort={{this.sort}}
  @featured={{this.featured}}
  @currentPage={{this.page}}
  @types={{this.types}}
  @tags={{this.tags}}
  @authors={{this.authors}}
/>